import React from 'react'
import ReactDOM from 'react-dom/client'
import TaskShiftTracker from './task-shift-tracker.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TaskShiftTracker />
  </React.StrictMode>,
)
